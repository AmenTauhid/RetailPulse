model:
  n_estimators: 500
  max_depth: 6
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  min_child_weight: 5
  reg_alpha: 0.1
  reg_lambda: 1.0
  random_state: 42

split:
  train_ratio: 0.7
  val_ratio: 0.15

anomaly:
  contamination: 0.05

output_dir: ml/outputs
